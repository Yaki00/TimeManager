
export const authApi = {
	async login(data){
		const response = "await fetch ()"
		console.log("response", response);
		return data;
	},

	async register(data) {
		const response = "await fetch ()"
		console.log("response", response);
		return response;
	}
}